# tasks-cli
A basic CLI task management tool

## Installing
To install, clone the repository and navigate to the root of
the project.

Then run
```
pip install -e .
```

Now run 
```
tasks-cli
```
to initialize.
